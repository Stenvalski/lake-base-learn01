[DEFAULT]
sources = migrations
# No password here: ./migrate supplies a fresh OAuth token through PGPASSWORD.
# %% is configparser's escape for a literal % (the @ in the username).
database = postgresql+psycopg://stenvall.dan%%40gmail.com@ep-curly-bread-d84i5di7.database.us-east-2.cloud.databricks.com/databricks_postgres?sslmode=require
batch_mode = on
verbosity = 0
